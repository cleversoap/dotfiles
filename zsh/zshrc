export XDG_CONFIG_HOME=$HOME/.config
export XDG_DATA_HOME=$HOME/.local/share
export XDG_CACHE_HOME=$HOME/.cache

# Base16 Shell
BASE16_SHELL=$XDG_CONFIG_HOME/base16-shell/base16-ocean.dark.sh
[[ -s $BASE16_SHELL ]] && source $BASE16_SHELL
