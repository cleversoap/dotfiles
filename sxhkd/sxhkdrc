# Should polybar and dzen kill come before bspc quit? Are they even necessary?
super + alt + Escape
    bspc quit && pkill polybar && pkill dzen

# Confirm that this environment variable is acceptable because XDG is only set
# in zprofile (although there's no harm in resetting it I guess in zshrc)
# however it may just be better to directly point to ~/.config and make sure
# to keep in sync
#
# It might be nice to try to use $TERMINAL here instead of st directly too
alt + Return
    st -e zsh -l -c "tmux -2 -f $XDG_CONFIG_HOME/tmux/tmux.conf"

# Open a browser - possible to use $BROWSER environment variable?
alt + shift + Return
    vivaldi-stable

# TODO: Just for debugging so that I can guarantee a terminal as st is still WIP
alt + t
    urxvt

# Save screenshot of desktop
Print
    mkdir -p $HOME/screenshots && scrot $HOME/screenshots/%Y-%m-%d-%T.png

# Save screen of focused window
alt + Print
    mkdir -p $HOME/screenshots && scrot -u $HOME/screenshots/%Y-%m-%d-%T.png
